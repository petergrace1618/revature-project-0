package site.petergrace.shivacorp.users;

public class Customer extends User {
    Account[] acctNumbers;
    // applyNewAcct(balance)
    // viewAcctBalance()
    // withdraw(amount)
    // deposit(amount)
    // postTransfer(amount, acct)
    // acceptTransfer(amount, acct)
}
